[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "propwatch"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
propwatch = "propwatch.cli:main"
